import React from 'react';


const OneCate = ({_id, name, image, duration}) => {
    return (
        <div>
            <div>
                
            </div>
            <h4>{name}</h4>
            <p>{duration} hour</p>
        </div>
    );
};

export default OneCate;